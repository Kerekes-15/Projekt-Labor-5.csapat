<Page
    x:Class="VTrailer.Presentation.AuditLog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"

    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Rendszer Napló (Audit Log)" FontSize="28" FontWeight="Bold" Margin="0,0,0,20" />

        <ListView x:Name="LogListView" Grid.Row="1" SelectionMode="None">

            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12" Background="#e0e0e0" CornerRadius="4,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Időpont" FontWeight="Bold" />
                        <TextBlock Grid.Column="1" Text="Felhasználó" FontWeight="Bold" />
                        <TextBlock Grid.Column="2" Text="Művelet" FontWeight="Bold" />
                        <TextBlock Grid.Column="3" Text="Tábla" FontWeight="Bold" />
                        <TextBlock Grid.Column="4" Text="Részletek" FontWeight="Bold" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,16" BorderBrush="#d3d3d3" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding CreatedAt}" Foreground="Gray" />
                        <TextBlock Grid.Column="1" Text="{Binding UserEmail}" />
                        <TextBlock Grid.Column="2" Text="{Binding Action}" FontWeight="SemiBold" Foreground="#d32f2f" />
                        <TextBlock Grid.Column="3" Text="{Binding TargetTable}" />
                        <TextBlock Grid.Column="4" Text="{Binding Details}" TextWrapping="WrapWholeWords" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
