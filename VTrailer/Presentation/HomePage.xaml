<Page
    x:Class="VTrailer.Presentation.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VTrailer.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <NavigationView x:Name="NavView" 
                  ItemInvoked="NavView_ItemInvoked" 
                  IsBackButtonVisible="Collapsed"
                  IsSettingsVisible="False">
        <NavigationView.PaneHeader>

            <Image Source="ms-appx:///Assets/logo.png" 
                 Stretch="Uniform" 
                 Height="120" 
                 Margin="-30,-30,0,12" 
                 HorizontalAlignment="Left"/>

        </NavigationView.PaneHeader>

        <NavigationView.MenuItems>
            <NavigationViewItem Content="Kezdőlap" Tag="Home" Icon="Home" />
            <NavigationViewItem Content="Kínálatunk" Tag="Trailer" Icon="List" />
            <NavigationViewItem Content="Foglalás" Tag="Booking" Icon="Calendar" />
            <NavigationViewItem Content="Foglalásaim" Tag="MyBookingsPage">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE81C;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem x:Name="AllBookingsMenu" Content="Összes foglalás" Tag="AllBookingsPage" Icon="Library" />
            <NavigationViewItem x:Name="AuditLogMenu" Content="Rendszer Napló" Tag="AuditLog" Icon="Document" />
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem Content="Fiók Beállítások" Tag="Settings" Icon="Setting" />

            <NavigationViewItem Content="Kijelentkezés" Tag="Logout" Foreground="IndianRed">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE7E8;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.FooterMenuItems>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Height="48" Padding="16,0" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource SurfaceVariantBrush}">
                <ToggleSwitch x:Name="ThemeToggle" OffContent="" OnContent="" Toggled="ThemeToggle_Toggled"
                              HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>

            <Frame x:Name="ContentFrame" Grid.Row="1" />

            <StackPanel x:Name="WelcomeTextPanel" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <TextBlock Text="Üdvözlünk a VTrailer Rendszerében!" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Válassz a bal oldali menüből az utánfutók megtekintéséhez vagy új foglalás rögzítéséhez." 
                           FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <FontIcon Glyph="&#xE80F;" FontSize="120" Margin="0,48,0,0" Foreground="#0078D7" Opacity="0.8"/>
            </StackPanel>
        </Grid>
    </NavigationView>
</Page>
