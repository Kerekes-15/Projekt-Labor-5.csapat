<Page x:Class="VTrailer.Presentation.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:VTrailer.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      Background="{ThemeResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Fiók Beállítások" FontSize="28" FontWeight="Bold" Margin="10,0,0,24" />

        <Grid Grid.Row="1" Margin="32,0,32,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="24" Width="400" HorizontalAlignment="Left" VerticalAlignment="Top">

                <FontIcon Glyph="&#xE77B;" FontSize="80" HorizontalAlignment="Left" Margin="0,0,0,10"/>
                <TextBlock Text="Személyes adatok" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />

                <StackPanel Spacing="16">
                    <TextBox Header="Teljes név" Text="{Binding FullName, Mode=TwoWay}" PlaceholderText="Kérlek add meg a teljes neved" HorizontalAlignment="Stretch"/>
                    <TextBox Header="E-mail cím" Text="{Binding Email, Mode=OneWay}" IsEnabled="False" HorizontalAlignment="Stretch"/>

                    <TextBox Header="Telefonszám" 
                             Text="{Binding PhoneNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             PlaceholderText="+36 30 123 4567"
                             PlaceholderForeground="Gray"
                             HorizontalAlignment="Stretch" 
                             MaxLength="16"
                             InputScope="TelephoneNumber"
                             BeforeTextChanging="PhoneNumber_BeforeTextChanging"/>

                    <TextBox Header="Jogosultság" Text="{Binding Role, Mode=OneWay}" IsEnabled="False" HorizontalAlignment="Stretch"/>

                    <TextBlock Text="{Binding StatusMessage}" Foreground="Green" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>

                    <Button Content="Adatok mentése" Command="{Binding SaveProfileCommand}" HorizontalAlignment="Stretch" Height="40" Background="#5b4bc4" Foreground="White"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Column="2" Background="#ffffff" CornerRadius="8" Padding="24" HorizontalAlignment="Left" VerticalAlignment="Top" Width="400" Margin="0,110,0,0">
                <StackPanel Spacing="16">
                    <TextBlock Text="Jelszó módosítása" FontWeight="SemiBold" FontSize="20" Foreground="#333333" Margin="0,0,0,10"/>

                    <PasswordBox Header="Új jelszó" 
                                 Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 HorizontalAlignment="Stretch" />

                    <PasswordBox Header="Új jelszó megerősítése" 
                                 Password="{Binding ConfirmNewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 HorizontalAlignment="Stretch" />
                    <TextBlock Text="{Binding PasswordStatusMessage}" Foreground="Green" FontWeight="Bold" HorizontalAlignment="Center" TextWrapping="Wrap" />

                    <Button Content="Jelszó frissítése" 
                            Command="{Binding ChangePasswordCommand}" 
                            Background="#5b4bc4" 
                            Foreground="White" 
                            Height="40"
                            CornerRadius="20"
                            HorizontalAlignment="Stretch"
                            Margin="0,16,0,0"/>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>
</Page>
