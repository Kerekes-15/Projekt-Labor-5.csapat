<Page
    x:Class="VTrailer.Presentation.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VTrailer.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid>
            <Button Command="{Binding GoToLoginCommand}" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Background="#5b4bc4" Foreground="White">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72B;" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="Vissza a bejelentkezéshez" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="15" Width="320">

                <TextBlock Text="VTrailer Regisztráció" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" />
                <TextBlock Text="Hozd létre a fiókodat!" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10" Opacity="0.7"/>

                <TextBox Text="{Binding FullName, Mode=TwoWay}" PlaceholderText="Teljes név" HorizontalAlignment="Stretch" />
                <TextBox Text="{Binding Email, Mode=TwoWay}" PlaceholderText="E-mail cím" HorizontalAlignment="Stretch" />
                <TextBox Text="{Binding PhoneNumber, Mode=TwoWay}" PlaceholderText="Telefonszám" HorizontalAlignment="Stretch" />

                <PasswordBox Password="{Binding Password, Mode=TwoWay}" PlaceholderText="Jelszó" HorizontalAlignment="Stretch" />
                <PasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay}" PlaceholderText="Jelszó megerősítése" HorizontalAlignment="Stretch" />

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}" 
                   HorizontalAlignment="Center" TextWrapping="Wrap"/>

                <Button Content="Regisztráció" Command="{Binding RegisterCommand}" 
        HorizontalAlignment="Stretch" Height="48" Margin="0,10,0,0" 
        Background="#5b4bc4" Foreground="White" CornerRadius="24"/>

            </StackPanel>
        </Grid>
    </Grid>
</Page>
