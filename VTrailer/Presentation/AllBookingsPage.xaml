<Page
    x:Class="VTrailer.Presentation.AllBookingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VTrailer.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Összes Foglalás Kezelése" FontSize="28" FontWeight="Bold" Margin="0,0,0,24" />

        <ListView x:Name="AllBookingsListView" Grid.Row="1" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{ThemeResource SurfaceVariantBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Ügyfél neve:" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding CustomerName}" FontSize="16" FontWeight="Bold" TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                                <TextBlock Text="{Binding TrailerName}" FontSize="15" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding DisplayDate}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding DisplayPrice}" FontSize="16" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" VerticalAlignment="Center" Margin="0,0,16,0"/>

                            <Button Grid.Column="3" Content="Visszavéve" Foreground="White" CornerRadius="6" VerticalAlignment="Center" Click="OnReturnTrailerClick">
                                <Button.Resources>
                                    <SolidColorBrush x:Key="ButtonBackground" Color="#2e7d32"/>
                                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#1b5e20"/>
                                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#0a3d0e"/>
                                </Button.Resources>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="EmptyStateText" Text="Jelenleg nincs egyetlen foglalás sem a rendszerben." 
                   FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Visibility="Collapsed"/>
    </Grid>
</Page>
